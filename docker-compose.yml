version: '3.8'
services:
  web:
    image: your-dockerhub-user/netflix-gpt:latest
    # If you want to build locally instead of pulling image, uncomment build section
    # build:
    #   context: .
    #   args:
    #     REACT_APP_TMDB_API_KEY: ${REACT_APP_TMDB_API_KEY}
    ports:
      - "80:80"
    restart: unless-stopped
